apiVersion: v1
kind: Secret
metadata:
  name: {{ include "airflow.name" . }}-secret
  labels:
    {{ include "airflow.labels" . | nindent 4 }}
type: Opaque
stringData:
  fernet-key: {{ .Values.airflow.config.core.fernetKey | quote }}
  database-url: {{ .Values.airflow.config.core.sqlAlchemyConn | quote }}
  postgres-password: {{ .Values.postgres.postgres.password | quote }}
  postgres-user: {{ .Values.postgres.postgres.user | quote }}
  postgres-db: {{ .Values.postgres.postgres.db | quote }}

